language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-lmn-theme' ]; then cd .. && eval "mv $currentfolder generator-lmn-theme" && cd generator-lmn-theme; fi
